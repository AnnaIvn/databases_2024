Лабораторна робота №2
З дисципліни Бази даних та інформаційні системи
Студента групи МІТ-31 Іваницької Анни

Опис бази даних: База даних складається з двох таблиць зі зв'язком one-to-one. 
Перша таблиця описує вид дерева (id, назва, наукова назва, способи використання, url зображення).
Друга - надає характеристики цього виду дерева (id, щільність, твердість, колір).

Було виконано:
1. розроблена схема таблиць на сайті dbdiagram.io
2. використаний сайт mockaroo.com для отримання даних
3. зроблено .sql файл з відповідними командами

Висновки: 
	В результаті виконання даної лабораторної роботи було опрацьовано базові команди для psql.
В командному рядку psql було створено нову базу даних lab1_wood, було створено два нових
користувача: user1, user2, - з паролями 1111 для кожного. Далі їм були надані різні рівні 
доступу: user1-доступ перегляду public schemas та доступ перегляду інформації з таблиць в 
public schemas, user2-all privileges for public schemas. 
	В новій базі даних було створено дві таблиці з one-to-one зв'язком та внесено в них дані
	Була додана ще одна таблиця wood_suppliers, і був встановлений зв'язок one-to-many з 
таблицями wood_types та wood_suppliers

Додатки:
1. wood types dbdiagram.io .pdf - діаграма бази даних зроблені на сайті dbdiagram.io
2. wood_tables.sql  - .sql файл з основними командами, що були використані
3. pre.sql  - .sql файл з базовими командами, що були використані
4. command prompt output.txt  - вивід основного під час процесу роботи в терміналі